server:
  port: 8888

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/Surya00011/algoRun.git
          default-label: main
          search-paths: algorun-config
